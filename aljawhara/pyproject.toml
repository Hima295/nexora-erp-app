[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "nexora"
authors = [
    { name = "nexora Engineering Team", email = "dev@nexora.internal" }
]
description = "Enterprise-grade custom Frappe application extending ERPNext v15+"
readme = "README.md"
dynamic = ["version"]
dependencies = [
    # Required base dependencies for ERPNext v15+ extension
]

[project.urls]
Homepage = "https://github.com/nexora-org/nexora"
Repository = "https://github.com/nexora-org/nexora.git"
