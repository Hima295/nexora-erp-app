[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "aljawhara"
authors = [
    { name = "Aljawhara Engineering Team", email = "dev@aljawhara.internal" }
]
description = "Enterprise-grade custom Frappe application extending ERPNext v15+"
readme = "README.md"
dynamic = ["version"]
dependencies = [
    # Required base dependencies for ERPNext v15+ extension
]

[project.urls]
Homepage = "https://github.com/aljawhara-org/aljawhara"
Repository = "https://github.com/aljawhara-org/aljawhara.git"
